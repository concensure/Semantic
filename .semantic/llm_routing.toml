# preferred: ordered list of provider keys from llm_config.toml
[planning]
preferred = ["primary", "secondary"]

[execution]
preferred = ["primary", "local", "secondary"]

[interactive] # ask mode (no code changes)
preferred = ["secondary", "primary"]
