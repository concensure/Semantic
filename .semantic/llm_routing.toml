# preferred: ordered list of provider keys from llm_config.toml
[planning]
preferred = ["anthropic", "openai"]

[execution]
preferred = ["openai", "ollama", "together"]

[interactive] # ask mode (no code changes)
preferred = ["openrouter", "openai"]
